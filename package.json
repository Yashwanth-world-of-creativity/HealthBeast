{
  "name": "healthbeast-workspace",
  "version": "1.0.0",
  "description": "Root workspace runner for HealthBeast AI",
  "scripts": {
    "dev": "python3 backend/app.py & npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
